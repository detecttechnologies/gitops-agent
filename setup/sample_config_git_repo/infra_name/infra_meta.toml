[app_name_here]
    code_url = "INSERT_GIT_REPO_SSH_URL_HERE"
    code_commit_hash = "INSERT_COMMIT_HASH_HERE"
    code_local_path = "Ex: /mnt/abc/def"

    # # Reference:
    # [APP_NAME_HERE]
    #     code_url = "INSERT_GIT_REPO_SSH_URL_HERE"
    #     code_commit_hash = "INSERT_HASH_HERE"
    #     code_local_path = "Ex: /mnt/abc/def, should be absolute"
    #
    #     # OPTIONAL: copy one or more config files into place after the code is updated.
    #     # `src` is RELATIVE to this deployment-config repo's root; `dst` is an ABSOLUTE path.
    #     # Destination parent dirs are created automatically; an entry whose `src` is missing is
    #     # skipped (with a log) instead of aborting the run.
    #     config_files = [
    #         { src = "infra_name/config.toml", dst = "/mnt/abc/def/config.toml" },
    #         { src = "infra_name/secrets.env", dst = "/mnt/abc/def/.env" },
    #     ]
    #
    #     # NOTE: the old single-file keys `config_src_path_rel_in_this_repo` and
    #     # `config_dst_path_abs` are NO LONGER SUPPORTED. If either is present the agent will raise
    #     # an error asking you to migrate to the `config_files` array above.
    #
    #     pre_updation_command = "OPTIONAL, Ex: git stash"
    #     post_updation_command = "OPTIONAL, Ex: docker restart xyz; git stash pop"
